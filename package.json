{
  "name": "labourlink",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run backend\" \"npm run frontend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "backend\\venv\\Scripts\\python backend\\app.py",
    "install:all": "npm install --prefix frontend && backend\\venv\\Scripts\\pip install -r backend\\requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
